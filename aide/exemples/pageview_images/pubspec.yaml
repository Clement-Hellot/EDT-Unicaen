name: pageview_images
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets: 
    - assets/
  # En fait c'est le chemin du dossier "assest"
  # On peut lister les assets (par exemple des images) un par un
  # ou alors mettre juste le dossier comme ça


  
